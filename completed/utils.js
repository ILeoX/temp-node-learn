const sayHi =  (name) => {
      console.log(`Hello ${name}`)
}

const thunder = (name) => {
      console.log(`Thunder fire you ${name}`)
}
const items = {hello: "hello", hi:"hi"}

module.exports = {sayHi, thunder,items}

